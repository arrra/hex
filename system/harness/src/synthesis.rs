/// Port of system/scripts/weekly-synthesis-digest.sh and synthesis-trigger.sh.
/// Thin wrappers — logic lives in the shell scripts; this module exists to satisfy
/// Rust's module system requirements for `mod synthesis`.
pub fn _placeholder() {}
